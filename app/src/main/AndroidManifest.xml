<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      The whole app hinges on this permission. Requested at runtime from the
      diagnostics screen (Phase 0) and later from the tuner screen (Phase 1).
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
      Advertise that we want low-latency + pro audio. Some OEMs (and the
      framework's own scheduling) use these flags as a hint that we're willing
      to trade a bit of power for a small audio buffer. Not required — just
      polite.
    -->
    <uses-feature android:name="android.hardware.audio.low_latency" android:required="false" />
    <uses-feature android:name="android.hardware.audio.pro" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".TrueTuneApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrueTune">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TrueTune">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
